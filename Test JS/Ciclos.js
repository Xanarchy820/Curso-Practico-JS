let iterador = 0;

while(iterador <= 4) {
    console.log("El valor de i es: " + iterador);
    iterador++;
}

iterador = 10;

while(iterador >= 2) {
    console.log("El valor de i es: " + iterador);
    iterador--;
}